# DKUCC 4×A40：动态 R-SWA SFT 与熵门控 RL 的兼容性评估

评估日期：2026-09-11。

结论：可以继续使用 DKUCC 的 4×A40 48GB 与 MOSS。建议将研究主线设为 **4 卡数据并行，sequence_parallel_size=1**，按需加入 ZeRO；固定/动态 R-SWA 的 attention 后端单独适配。现成的 MS-Swift 多模态序列并行 + FlashAttention 组合，不能直接视为支持任意动态 KV mask。熵门控 RL 在原理上与 FlashAttention 兼容，主要改造位于逐 token 概率、advantage、loss 与 rollout 一致性。

本文为文档和源码评估。没有在本次评估中连接 DKUCC、安装软件或运行多卡动态 mask / RL 实验。

**证据范围。** 已读取关联任务《整理妙记并推进语音项目》、原执行文档、完整逐字稿相关段落及当前工作区运行记录。MS-Swift 以当日主分支 commit `0673cf75dca7d0b9b608b4a76632fb508ead5076` 为源码参照；MOSS 以环境记录中的 commit `61bc29cd4120be7b5d3b761b64cd5dff57263642` 为参照。DKUCC 实际安装的 MS-Swift/FlashAttention 版本仍须在实施时核验。

**已有本地证据。** [环境记录](./environment.md)记载 A40、单作业最多 4 卡，以及双卡 NCCL 通信通过；[SFT 记录](./smoke-sft-result-2026-09-11.md)记载官方 finetune.py 单卡两步训练及保存通过。它们支持继续开展适配，尚不构成 4 卡训练、MS-Swift 注册或动态 R-SWA 通过的证据。

## 1. 先分清三类机制

| 机制 | 改变什么 | 对本项目的影响 |
|---|---|---|
| 数据并行 DDP | 每卡处理不同样本，再同步参数梯度 | 自定义 attention 在每卡本地运行，通常不要求改变 mask 语义 |
| ZeRO / FSDP | 切分优化器、梯度、参数等模型状态 | 一般可以结合自定义 attention；参数访问、重算、保存仍需适配测试 |
| 序列并行 Ulysses / Ring | 同一样本的序列或 attention 计算跨卡分布 | mask、位置、样本边界、跨卡通信及反向传播均须配合 |
| attention mask | 某个 query 可以读取哪些 key/value | 实现固定/动态 R-SWA |
| RL loss mask | 哪些输出位置参与某项策略损失 | 实现文本/结构 token 分流及高熵 token 筛选 |

DDP 不会把 4×48GB 自动合成单样本可用的 192GB。ZeRO 主要减少模型状态占用，不能自动把一条长音频的全部激活按四等分。以上划分依据 [PyTorch DDP](https://docs.pytorch.org/docs/2.14/generated/torch.nn.parallel.DistributedDataParallel.html)和 [DeepSpeed ZeRO](https://deepspeed.readthedocs.io/en/latest/zero3.html)。

原文档所说的序列并行，是 Transformers 路线的 `sequence_parallel_size`；Megatron-SWIFT 的同名近似选项具有不同语义，实施时不能混用。

## 2. FlashAttention 的边界

A40 属于 Ampere，位于 FlashAttention-2 支持范围内。FA3 的官方 Hopper 实现要求 H100/H800；不能把 Unlimited OCR 的 FA3 速度实验直接套到 A40。硬件支持也不等于当前 torch/CUDA/扩展版本已完成编译和验证。[FlashAttention 官方说明](https://github.com/Dao-AILab/flash-attention)

标准 FA2 接口擅长 causal、padding/varlen、固定 sliding window 等结构。R-SWA 要表达“完整 reference + 最近 k 个输出”，普通的单一 sliding_window 会连较早的 reference 一并排除。逐 query、逐 head 或依内容变化的窗口还增加了额外表达需求。

Transformers 的 FA2 路径使用其规定的 mask 格式，不接受任意原始 4D attention mask；SDPA 与 FlexAttention 分别有张量 mask、BlockMask 等接口。因此，仅替换一个传给 FA2 的 attention_mask 参数，不足以实现动态 R-SWA。[Transformers attention 接口](https://huggingface.co/docs/transformers/attention_interface)

这不是 FlashAttention 算法在数学上排斥 R-SWA：可以设计相应稀疏内核或改造组合计算。关键在于当前调用接口、内核的前向/反向和分布式封装是否共同支持目标模式。

对于训练，建议：

- 小序列正确性参考：显式 additive mask + eager / SDPA math；被屏蔽项在 softmax 前加负无穷。
- 固定/规则动态窗口的高效候选：FlexAttention 的 mask_mod / BlockMask，逐层接入 MOSS 的文本 decoder。
- 接入 FA2 或其他优化内核时，要求与参考结果及梯度一致，再比较性能。

SDPA 可能根据 mask、dtype、形状退回其他实现；选择 SDPA 不代表一定使用 flash 内核。[PyTorch SDPA](https://docs.pytorch.org/docs/2.14/generated/torch.nn.functional.scaled_dot_product_attention.html) FlexAttention 支持可编程 attention，但仍应按具体 PyTorch、GQA、checkpointing 与 A40 组合测试。[PyTorch FlexAttention](https://docs.pytorch.org/docs/main/nn.attention.flex_attention.html)

**两项容易混淆的实现：**

1. 把 K/V 数值置零不等于把对应 attention 项屏蔽。尤其 K 置零后仍可能获得 softmax 概率质量。
2. 生成时裁剪 KV cache 与 SFT 的可微前向不是同一个接口。FA2 的 `flash_attn_with_kvcache` 明确不支持 backward，不能直接把它搬进 SFT。[官方接口](https://github.com/Dao-AILab/flash-attention#how-to-use-flashattention)

## 3. Unlimited OCR 与动态窗口的关系

Unlimited OCR v1 的 R-SWA 使用固定输出窗口，默认 128，并始终保留 reference。论文报告多卡 Megatron-LM 训练，这说明 R-SWA 与多卡训练能够共存；论文不构成“MS-Swift + MOSS + 动态窗口”已实现的证据。[论文 §3.4、§4.2](https://arxiv.org/html/2606.23050v1)

在预测第 t 个输出 token 时，我们希望可见集合为：

```text
V_t = 完整音频与指令 reference
      ∪ {此前最近 k_t 个已生成输出 token}
```

训练中需将此定义映射到 shifted input/label 的 query 行：可见当前输入位置，不能看到要预测的目标 token 或更晚位置。prefix 内部保持基线的合法因果关系；只有后续输出位置可以看到完整 prefix。packing 时还必须限制在同一个样本内。

“动态”至少分成以下几种，工程难度不同：

| 动态方案 | SFT 可行性 | 关键要求 |
|---|---|---|
| 每条样本或每步随机选择一个 k | 较容易 | mask 可在前向前生成；checkpoint 重算复用同一选择 |
| 根据已知片段边界或过去状态调整 k_t | 可行 | 决策仅依赖生成当时可见信息；训练与生成规则一致 |
| 每个位置/head 学习硬选择或 top-k | 需要额外训练方法 | 离散选择本身不能直接由普通 SFT 梯度学习 |
| 学习 soft gate / attention bias | 可微候选 | 核验内核对 gate/bias 参数反向的支持，并处理训练到硬裁剪的差异 |

hard mask 可以让主模型正常 SFT，但“模型在这种 mask 下学习”与“mask 选择器也被训练”必须分别验证。训练可学习选择器可考虑连续松弛、直通估计或策略梯度；这些都是新增研究设计。

若使用“当前输出分布的熵”决定当前层的窗口，会形成计算依赖环：需要先完成前向才能得到熵。可先用上一步熵，或明确设计额外探测前向，并计入成本。训练时不能利用当前目标/未来标签来生成推理时不可获得的窗口。

**动态 cache 的重要限制。** 若先将输出 cache 裁到 128，再让窗口扩大到 512，先前被淘汰的 KV 已不存在。直接从训练中的完整历史重新读出这些位置，会造成训练/推理不一致。可选实现：

- 保存最近 k_max 的实际 cache，k_t 只控制可见范围；内存按 reference + k_max 计。
- 真正淘汰后不允许重新访问，策略必须遵守可用历史。
- 从保留状态重算被淘汰的 KV，明确计入额外延迟。

第一版推荐保存 k_max，以便验证动态策略。其代价是物理 cache 不会随每步较小的 k_t 同步缩小。

保留全部音频 reference 后，解码 cache 上界依然含 reference 长度。R-SWA 控制的是“随输出增长的历史成本”，不能据此保证任意长输入在 48GB 内训练。SFT 还保存激活和梯度，不能套用解码 cache 的常数内存结论。

## 4. MS-Swift 序列并行为什么需单独改造

当前官方 FAQ 仍写明：多模态模型的序列并行仅支持 flash-attn；自定义 loss 可能被序列并行自身路径替代。[固定版本 FAQ Q16](https://github.com/modelscope/ms-swift/blob/0673cf75dca7d0b9b608b4a76632fb508ead5076/docs/source_en/Instruction/Frequently-asked-questions.md#q16-sequence-parallel-related-issues)

源码会包装 FlashAttention/SDPA 调用，并处理 QKV 重分布、position_ids 和变长序列；Ring 分支还调用专门的 attention 实现。这种封装没有自动解决任意动态 R-SWA 的全局可见性。[sequence_parallel.py](https://github.com/modelscope/ms-swift/blob/0673cf75dca7d0b9b608b4a76632fb508ead5076/swift/sequence_parallel/sequence_parallel.py)

若必须接入 SP，需要逐项处理：

- Ulysses 交换后的完整序列和本地 head 编号；窗口及 reference 元数据必须对应交换后的布局。
- Ring 接收到远端 KV 块时的原始全局位置；窗口不能用每卡从零开始的局部下标计算。
- reference 跨卡可访问，且不会因全局前缀保留而跨到其他 packed 样本。
- causal、padding、样本边界、GQA、梯度重算同时正确。
- 自定义 loss 的 shift、gather 和 reduction 路径确实生效。

因此，单卡动态 mask 跑通并不意味着打开 SP=4 就可训练。但 SP 也不是不可做：它是后续需要明确验收的工程阶段。

## 5. 熵门控 RL 与 FlashAttention 基本兼容

逐字稿 00:24:33–00:29:23 描述的是：依据文本/时间戳/说话人等 token 类型，再结合输出预测熵，选择参与 GRPO 类更新的位置。

这里的熵来自词表分布：

```text
p_t = softmax(logits_t / temperature)
H_t = -sum_v p_t(v) log p_t(v)
```

它不需要 attention 权重矩阵。因此 FlashAttention 不显式保存完整 QK softmax 矩阵，并不会阻断这一熵计算。若将研究改为“attention 分布熵”，才需要另外设计统计内核或重算。

这也不同于直接把熵加进 reward。会议方案中，CER/DER 等提供任务质量信号，熵用于选择 loss 位置。高熵并不自动意味着该位置的预测错误；有效性仍需说话人日志实验检验。

**已存在的框架能力。** MS-Swift 有 `top_entropy_quantile`；设为 0.2 可作为高熵筛选基线。[官方熵门控文档](https://github.com/modelscope/ms-swift/blob/0673cf75dca7d0b9b608b4a76632fb508ead5076/docs/source_en/Instruction/GRPO/AdvancedResearch/entropy_mask.md)

**源码发现，不能直接照搬为项目最终算法：**

- GRPO 的 SP 路径会从 logits 算 entropy，再经 GatherLoss 收集；SP 本身没有让熵不可获得。
- 当前阈值直接对本次 loss 调用的 entropies 做 nanquantile。DDP 各进程的独立数据未在该处先合并，故不能把它当作四卡全局 token 的 top 20%。
- entropy mask 乘在策略项上，随后才加 KL 项；未选中的位置仍可能贡献 KL。
- 该开关不自动完成 CER/DER 分别归一化 advantage 和按 token 类型路由。

源码位置：[GRPOTrainer 阈值与 loss](https://github.com/modelscope/ms-swift/blob/0673cf75dca7d0b9b608b4a76632fb508ead5076/swift/rlhf_trainers/grpo_trainer.py#L939)、[SP log-probability / entropy](https://github.com/modelscope/ms-swift/blob/0673cf75dca7d0b9b608b4a76632fb508ead5076/swift/rlhf_trainers/grpo_trainer.py#L1454)。本次已核验固定 commit 的文件与读取的 main 内容相同。

建议的项目实现契约：

1. 同一 prompt 的多条 rollout 构成 GRPO group，分别计算 CER reward 与 DER reward 的组内 advantage。明确标准差归一化及零方差处理。
2. 在原始 token IDs 上保留文本、时间戳、speaker、EOS 等位置标签；字符串正则匹配后仍需准确映射 tokenizer 位置。
3. 分别构造 text gate 与 structure gate，再组合各自的策略损失。不要先把两项 reward 合成单一标量后期待框架自动分配。
4. 冻结熵门控的梯度；明确是全体有效输出的 global top 20% 再与类型相交，还是各类型内部各取 top 20%。后者可能有助于避免少量结构 token 被挤掉，但属于不同实验。
5. 若目标是跨卡 global top 20%，收集去重的有效 token 熵或分布统计，再同步阈值；不用收集整个词表 logits。DP、SP 混合时防止 SP 副本重复计数。
6. 明确统计 batch 是单次微批还是含梯度累积的有效 batch。单次前向无法自动获得以后微批的熵。
7. loss 分母、有效 token 数和分布式梯度平均按同一目标归一化；某卡没有选中 token 时仍应保留合法计算图并参与通信。
8. KL 是否也门控必须显式定义。loss gate 只控制该输出位置的直接策略项；共享参数和它对后续 token 的影响仍可获得梯度。

初版可以沿用 GRPO 的有效 completion 长度作为分母，将“按选中 token 数重新归一化”另作消融，避免筛选比例与更新幅度同时变化。cpCER/Δcp 同时涉及文本与 speaker 对应关系，不能未经分析就当作纯 CER 分支处理。

**logits 的资源成本。** 熵需要完整分布或数学等价的精确归约，单个 sampled-token log-probability 不能确定词表熵。建议分块计算并仅保存每个 token 的 entropy/log-probability，不长期保留全部 logits；若内核已融合投影和损失，需确认它额外输出所需统计。原有 FAQ 提示 Liger 与 log_entropy 存在不兼容，不能直接照搬所有显存优化开关。[FAQ Q39](https://github.com/modelscope/ms-swift/blob/0673cf75dca7d0b9b608b4a76632fb508ead5076/docs/source_en/Instruction/Frequently-asked-questions.md#q39-is-it-possible-to-use---use_liger_kernel-and---log_entropy-together)

## 6. SFT 与 RL 相接处的最大风险

rollout 生成、old-policy log-probability 和当前策略重算必须采用一致的模型结构、reference 定义、动态 mask 决策语义与位置编码。

例如，训练采用 R-SWA，rollout 的 vLLM 仍用 full attention，就不是同一策略；重要性采样修正不能代替 attention 架构适配。vLLM 支持原版 MOSS，也不代表支持修改后的动态 R-SWA MOSS。

第一版推荐用相同 Transformers 自定义模型完成生成与重算。优化后的 cache decode 可以使用不同内核，但须与训练后端在同样历史、位置和 mask 下数值一致。

- 规则动态策略：规则只能依赖生成时已有状态，重放时复用规则或记录的决策。
- 随机窗口策略：记录并重放随机决策，checkpoint 重算也要一致。
- 学习型离散窗口控制器：若它也是策略的一部分，须定义其动作概率和训练目标；仅计算文字 token 的 GRPO ratio 不足以宣称训练了控制器。
- 若参考模型仅承担 KL 正则，其角色可另行定义；第一版建议复制相同 R-SWA 结构，减少不必要变量。
- 自定义 k 配置、gate 参数、缓存规则与 adapter 应随 checkpoint 保存，重载后核验。

## 7. 4×A40 的推荐实施顺序

下表为方案建议，不代表这些组合已经完成 DKUCC 验证。

| 阶段 | 建议配置 | 进入下一阶段的条件 |
|---|---|---|
| full-attention baseline | 4 卡 DDP，SP=1，bf16，gradient checkpointing；按需 ZeRO-2 | 同一有效 batch 的单卡与多卡损失/梯度对应，能保存和恢复 |
| 固定 k R-SWA | 先单卡小序列参考，再 4 卡 DDP + 可编程后端，k=128/256 | mask、梯度、cache decode 与 teacher forcing 一致 |
| 规则动态 k_t | 保留 k_max cache，保持 DDP、SP=1 | 窗口扩大/缩小、断点恢复和重算全部一致 |
| 熵门控 GRPO | 4 卡 DDP，匹配的 Transformers rollout，小 generation group 起步 | reward、advantage、类型 gate、跨卡阈值与归一化验证通过 |
| 长序列 SP 优化 | 先 SP=2、DP=2，再按收益考虑 SP=4、DP=1 | 全局 mask 与参考实现一致，并实测内存/吞吐收益 |

MOSS 的文本 decoder 使用 Qwen3；当前源码默认 16 个 query heads、8 个 KV heads，数量上可被 2/4 整除。这只消除了一个潜在的 Ulysses 约束，实际 checkpoint 配置及自定义多模态注册仍需验证。[MOSS 配置](https://github.com/OpenMOSS/MOSS-Transcribe-Diarize/blob/61bc29cd4120be7b5d3b761b64cd5dff57263642/moss_transcribe_diarize/configuration_moss_transcribe_diarize.py)、[MOSS 模型](https://github.com/OpenMOSS/MOSS-Transcribe-Diarize/blob/61bc29cd4120be7b5d3b761b64cd5dff57263642/moss_transcribe_diarize/modeling_moss_transcribe_diarize.py)

不要预设 4 卡互联带宽或线性加速。先在同一 4 卡作业中核验拓扑与通信，尤其关注序列并行所增加的逐层通信。当前工作区只记录了双卡通信通过。

如果单条长音频即使使用高效 attention 和重算仍超出 48GB，就需提前适配 SP，或先在较短样本验证方法。按音频切片会改变任务历史，应作为明确的数据处理方案，不能隐式改变 baseline。

## 8. 最小验收清单

1. **Mask 正确性：** reference 完整保留、future/padding/其他样本不可见；覆盖窗口边界、不同 prefix 长度和 k_t 变化。
2. **可微性：** 小张量下核对输出及 Q/K/V 梯度；可学习 gate 还要单独证明参数获得有效梯度。
3. **训练/生成一致：** 固定输出历史下比较 teacher forcing 与逐 token cache decode 的 logits，包含窗口先缩后扩的情形。
4. **多卡一致：** 固定样本与有效 batch，比较 1/2/4 卡对应损失和梯度；避免把不同 batch 的数值差异当成 bug。
5. **RL 路由：** 仅开启 CER 或 DER 时，对应直接策略项只出现在指定类型；测试 padding、EOS、零奖励方差和无选中 token。
6. **全局熵阈值：** 构造卡间熵分布明显不同的样本，证明结果是既定的跨卡 quantile，而非各卡局部筛选；验证 ties 处理。
7. **Rollout 重放：** 更新前同一模型的 rollout / old-logp 重算一致；窗口元数据、位置编码和 checkpoint 重载一致。
8. **资源及效果：** 分别记录 prefix/output token 长度、峰值显存、attention/LM-head/rollout 耗时、RTF 与 CER/DER/cpCER/Δcp，按音频时长分桶。

建议对原执行文档的核心修订是：**把“有多卡后启用序列并行”改成“默认用多卡数据并行；序列并行仅在长序列内存需求成立且动态 attention/loss 验收通过后启用”。** 4×A40 足以开始这条研究路线；后续能处理的最长音频和 RL 并发量由实际显存剖析确定。

