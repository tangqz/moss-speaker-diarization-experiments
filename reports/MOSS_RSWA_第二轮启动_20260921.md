# MOSS R-SWA 第二轮：R256 / R512，LR 1e-6

用户于 2026-09-21 授权关闭旧轮，清理无用 checkpoint，并启动下一轮。旧轮剩余消融、Test、性能测试均已取消；旧轮完整 Test 结果不存在，不能补写为完成。

## 第二轮冻结设置

| 项目 | 设置 |
|---|---|
| 初始权重 | 两组均从同一原始 MOSS Base 初始化，重新建立优化器与调度器 |
| 输出窗口 | R256、R512；完整音频与指令 reference 保留，绝对位置不重置 |
| 训练 | 全参数、teacher-forced CE、SP4 / FSDP1、BF16 计算、FP32 参数 |
| 学习率 | 初始 1e-6，134 steps 内线性衰减到 0；无 warmup |
| 数据 | 沿用 536 场完整训练会议，seed 0，有效 batch 4；1 epoch = 134 updates |
| checkpoint | 每 25 steps 保存；另存工程检查用 step 1 和 epoch 末 step 134 |
| 哨兵 | 新 4 场；25、75 步单独评测，50、100、125 步包含于完整 Dev |
| 完整 Dev | 50、100、125 步，每次 26 场，含生成、CE 和结构失败检查 |
| Test | 固定 epoch 末 step 134；56 场（AliMeeting 20、AMI 16、AISHELL-4 20） |
| 推理 | greedy，repetition penalty 1.02，仅作用于全部已生成 token；原有输出上限与指标口径不变 |
| 停止条件 | 完成一 epoch；工程异常、非有限梯度和资源故障会停止。无自动延长或第二种子 |
| 保留策略 | 最近两份完整恢复状态；25/50/75/100/125/134 的推理权重；评测后删除重复导出权重 |

新哨兵仅按 Dev 参考元数据选定，排除旧轮所有 6 场，再按每个语料剩余样本的时长三分位点选两场，未使用输出表现选择：

- AliMeeting：R8007_M8011、R8009_M8019。
- AMI：ES2011c、IB4003。

旧轮 Base Dev 已按同一模型、完整注意力后端、输入、penalty 和评分流程完成，因此复用其 26 场预测并记录来源与哈希。共享 Base Test 在本轮重新生成。两个训练模型固定用 epoch 末权重测 Test，不按 Test 选 checkpoint；该 Test 是此前使用过的官方测试集，不是新的盲测集。

## 调度与预计完成时间

账户实时限制是 `MaxJobs=1`、`MaxTRES gres/gpu=4`，因此不能同时运行两个四卡任务。最初提交的 64635/64636/64637 已在第一个优化器更新正式提交前取消，记录和日志保留。正式串行作业为 **64638**，使用节点 `dkucc-core-gpu-dkurc-d-it09-8` 的四张 A40。

顺序：R512 增量工程核验 → R256 训练和 Dev → 共享 Base Test → R256 Test → R512 训练和 Dev、Test。工程核验覆盖 R512 的推理核、缓存回收、penalty、SP4 前向/梯度，以及跨两次窗口的 1026-token 真实 MOSS 推理回放；通过后接续正式训练。

旧轮 R256 前 134 步实测纯更新耗时为 **1.984 小时**（每步均值 53.31 秒），不包括加载、保存和评测。考虑两组串行、多轮生成、CE、Test 与工程核验，初估 **北京时间 2026-09-22 06:00–09:00** 完成；R512 的实际速度、生成长度及工程检查可能使时间变化。此时间是估计，不是完成承诺。

## 旧轮清理记录

停止作业 64633，并清空旧轮后续队列。保留全部源代码、配置、训练日志、原始预测和指标；另保留 Full step 30、R128 step 30、R256 step 150 三份原生推理权重。其他 checkpoint 大文件与重复推理权重已清理。

- 删除 301 个文件，逻辑字节合计 **404.10 GiB**。
- 同一目录的 `du` 统计从 **585450184 KiB** 降至 **19413168 KiB**，即约 **558.33 → 18.51 GiB**；与逻辑文件大小是不同的存储统计口径。
- 清理前证据压缩包为 **311773938 bytes**，保存在旧轮 `closure/evidence_before_cleanup.tar.gz`；SHA-256 与逐文件清单写入 cleanup receipt。
- 旧轮剩余 Test 与性能实验取消，现有输出只用于收尾报告。

CE 或 S 下降不足以单独证明“尚未拟合”；本轮检验更大窗口与更高学习率组合能否同时改善识别和生成稳定性。因为两个超参数一起变化，不能把与旧轮的差异单独归因于学习率。

## 文件与远端位置

- 远端新轮：`/work/qt28/moss/results/moss-rswa-r2-20260921`。
- 实现：[第二轮脚本目录](../rswa_round2/)。
- 冻结协议：[protocol.json](../rswa_round2/evidence/protocol.json)。
- 作业记录：[launch.json](../rswa_round2/evidence/launch.json)。
- 旧轮删除明细：[round1_cleanup_receipt.json](../rswa_round2/evidence/round1_cleanup_receipt.json)。
- 旧轮总结：[收尾归档](../rswa/closure_20260921/MOSS_RSWA_旧轮收尾归档_20260921.md)。

运行状态以远端 `status-256.json`、`status-512.json`、作业日志和最终 `summary.json` 为准。现有可见 SSH 窗口继续接收本轮日志。

## 18:55 工程放行记录

R512 推理核、KV 回收、输出侧 penalty 与 SP4 前向/梯度检查通过。真实 MOSS 1026-token 回放的首选 token 一致率为 **99.51%（1021/1026）**，高 margin（>0.5）分歧为 0；全部 28 层 reference KV 保持不变，HF 缓存及 vLLM 物理页有界。

原有严格 BF16 误差阈值在 token 0、256、1025 三个采样位置失败，`hf_alignment.json` 的原始 `passed=false` 保留。此轮采用预先写入运行器的增量放行条件（独立数学核验、SP4 梯度、缓存边界、无高 margin 分歧），不宣称跨框架逐位一致，也不把该工程核验当作识别质量合格。记录见 [原始回放](../rswa_round2/evidence/r512_hf_alignment.json) 和 [放行收据](../rswa_round2/evidence/r512_complete.json)。

完成核验后，作业 64638 已进入 R256 的首步训练阶段；R512 正式训练将在 R256 阶段完成后接续。

18:58 已完成 R256 首个真实更新并保存完整 checkpoint-1（权重、optimizer、scheduler、四卡 RNG）。实际处理 4 场会议，四卡顺序检查通过，含加载和保存共 176.6 秒；随后进入 step 1 → 25 阶段。首步收据见 [checkpoint 验证](../rswa_round2/evidence/r256_checkpoint1_ready.json)。

18:59 已核实从 checkpoint-1 恢复：`starting_global_step=1`，两个优化器参数组的恢复学习率均为 `9.925373134328357e-7`，符合 134 步线性调度。记录见 [恢复运行证据](../rswa_round2/evidence/r256_resume_runtime.json)。
