# MOSS 全注意力 SFT 训练完成检查

检查时间：2026 年 9 月 12 日北京时间 10:30–10:33。证据来自重新认证后的 DKUCC SSH、Slurm 历史记录及本次训练的原始日志。

作业 **62994 已正常完成**：Slurm 状态 `COMPLETED`，退出码 `0:0`。北京时间 9 月 11 日 23:30:09 开始，9 月 12 日 03:33:23 结束，作业总耗时 4 小时 3 分 14 秒。正式训练计时为 3 小时 58 分 12 秒，其余时间包括启动及验证。

| 检查项 | 结果 |
|---|---|
| 训练进度 | 402 / 402 步，3.0 / 3.0 epoch |
| 配置 | 4 卡 DDP、SP=1、全注意力 SFT |
| 全程平均训练 loss | 0.430814 |
| 最后一步训练 loss | 0.447008 |
| 非有限 loss / 梯度范数 | 402 条记录中均未发现 |
| 训练异常 | stderr 未发现 Traceback、CUDA OOM 或 NCCL error |
| 验证集评估记录 | 0 条 |

每轮平均训练 loss（对该轮 134 条步日志取算术平均，并非额外进行的验证集评估）：

| 轮次 | 平均 loss |
|---|---:|
| 1 | 0.440927 |
| 2 | 0.426721 |
| 3 | 0.424793 |

第三轮比第二轮的平均训练 loss 下降约 0.45%，后期下降幅度已变小。学习率也按计划衰减至接近零。当前证据支持训练按配置完成，但不足以确认验证集收敛或识别、说话人区分能力提升；尚需进行独立验证集 CER、DER、cpCER 评估并与原始模型比较。

最终模型目录：`/work/qt28/moss/checkpoints/full-attention-ddp4-62994/`。

最终模型及 `checkpoint-400`、`checkpoint-402` 均存在，各自 `model.safetensors` 大小为 1,817,113,608 字节，包含 683 个张量条目，头部声明的数据结束位置与实际文件大小一致。两个检查点均包含优化器、学习率调度器及四个 rank 的随机数状态文件；保存步数分别为 400、402。此检查核对文件结构与大小，未重新加载全部模型执行推理。

本地原始证据位于 `dkucc/logs/completed-62994/`：

- `sacct.txt`：调度器完成记录，原始时间为服务器时区 UTC−04:00。
- `trainer_state.json`、`train_results.json`、`memory_sft_config.json`：完整步日志及配置。
- `training.out`、`training.err`：原始训练输出。
- `summary.json`：训练指标统计。
- `checkpoint-check.jsonl`：模型和检查点结构检查结果。

本次只进行了状态核查与证据保存，没有提交新训练或评估作业。当前交互登录连接使用控制路径 `C:/Users/qizhi/.ssh/cm-moss-check-20260912`，原先的 `cm-moss` 连接已失效。
