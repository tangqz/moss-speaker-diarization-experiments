# DKUCC 正式训练启动记录

2026-09-11 23:35（北京时间）已确认：**正式 full-attention SFT 已启动并完成首个优化器更新**。作业在服务器后台继续运行，不依赖当前 SSH 会话。

随后复查已完成 **4 个正式训练步**：第 4 步 loss=0.4957、grad_norm=1.742，作业仍为 RUNNING。本地与服务器训练代码 SHA-256 一致：`3e4a1618914e211bf339d22e21a4018c6e5eede408c07a6c18f8a6e6662c892f`。

## 当前训练

| 项目 | 已核实配置 / 状态 |
|---|---|
| Slurm 作业 | **62994**，检查时为 RUNNING |
| 节点 | `dkucc-core-gpu-dkurc-d-it09-3` |
| GPU / 并行 | 4×NVIDIA A40，DDP=4，SP=1 |
| 模型 | MOSS-Transcribe-Diarize，原始本地权重 |
| 数据 | `train_mix.jsonl`，536 条完整会议，约 299.6 小时 |
| 训练量 | 3 epochs，402 个优化器更新；单卡 batch=1，有效 batch=4 |
| 初始学习率 | 1e-5 |
| Attention | SDPA full attention；未启用 R-SWA 或动态 mask |
| 精度 / 重算 | bf16，gradient checkpointing |
| 最大长度 | 131072；数据实际最大 116527 token，无切片或隐式截断 |
| 保存 | 每 25 步保存，保留最近 2 个训练 checkpoint；结束时保存最终模型 |
| 首步证据 | 四个 rank 均完成 step=1；loss=**0.5258**，grad_norm=**5.125** |
| 首步进程峰值显存 | rank 0/1/2/3：8.752 / 13.524 / 21.384 / 13.188 GiB |

峰值数据仅描述该训练步，不代表后续所有会议的上限。GPU 0 另有 22.07 GiB 驱动残留占用，不计入上述训练进程峰值。

## 交接与修复

读取了 Proma 当前「说话人日志任务执行」会话（`d8c1327b-2681-4681-9505-72a9b12d846e`，更新于 22:45:58），以及 Codex「评估多卡训练与动态KV Mask兼容性」任务和其产出的 [兼容性评估文档](./A40_动态RSWA与熵门控RL_兼容性评估_2026-09-11.md)，按文档第 7–8 节执行。

1. 通过 OpenSSH `-O proxy` 成功复用 Proma 已认证的 SSH 主连接。
2. 将词表投影与 FP32 交叉熵一起分块重算，SFT 只返回 loss，避免保留完整 logits 及其 FP32 副本。原损失 shift 与跨卡有效 token 归一化保留。
3. GPU 0 存在驱动中可见、系统进程表中不存在的 PID 116725，占用 22602 MiB。对 rank 0 启用 backbone 激活卸载至 CPU，保留四卡数据并行。
4. Whisper 仅将逐 token 的前馈网络 `fc1 → GELU → fc2` 分批重算，默认每批 8 个模型原有音频块。Attention 的批次和整段会议的拼接、顺序、内容保持不变。

修复均位于独立训练入口中的当次模型实例。模型参数结构、无标签 eval 推理路径和已安装的 Transformers/Accelerate 文件保持原状。Whisper 层实现按当前 Transformers 5.17.0 核对，升级版本时需要重新验证。

## 已完成验证

- 同一四卡作业内的拓扑、NCCL all-reduce、broadcast 均通过。
- 单卡和四卡的分块损失、梯度、跨卡归一化验证通过，包括全忽略样本和不等长有效标签。
- 真实 MOSS 短音频对照：原版与修改版 loss 均为 0.5575617，无标签 eval 推理输出完全相同；BF16 绑定权重梯度相对差异约 1.69%，低于设定的 3% 容差。
- Whisper 前馈分批对照：输出完全一致；conv1、首层 Q 投影、首层 fc1 的梯度相对差异为 2.109%、0.481%、0.708%，低于设定的 BF16 4% 容差。
- 全量 536 条训练记录去重、音频路径、采样率、EOS、有效监督和精确长度检查通过。与各验证/测试集的音频路径交集为 0。最短与最长样本的长度还与完整音频特征处理结果交叉核验。
- 包含最长会议的八条样本完成四卡两步训练，保存模型、优化器、调度器和四个 rank 的随机状态。
- 作业 62994 从上述 checkpoint-2 恢复并完成第 3 步，再次保存状态，日志出现 `CHECKPOINT_RESUME_PASS step=3`。

恢复试跑沿用已经结束的两步学习率调度，第 3 步学习率为 0；它验证加载、前反向、状态延续和再保存，不用于证明延长训练时的学习率设置。正式 baseline 独立从原始权重启动，首步实测学习率为 1e-5。

## 运行路径与复现

- 正式输出：`/work/qt28/moss/checkpoints/full-attention-ddp4-62994/`
- 当前日志：`/work/qt28/moss/logs/moss-fa-ddp4-62994.out` 与 `.err`
- 恢复验证输出：`/work/qt28/moss/checkpoints/memory-smoke-62994/`
- 当前提交脚本：`/work/qt28/moss/dkucc/memory_sft/resume_audio.slurm`
- 本地实现与说明：[memory_sft/README.md](./memory_sft/README.md)
- 数据核验：[preflight-62990.json](./memory_sft/preflight-62990.json)
- 正式配置：[baseline-config-62994.json](./memory_sft/baseline-config-62994.json)
- 恢复状态：[resume-state-62994.json](./memory_sft/resume-state-62994.json)

已核对的代码版本：`OpenMOSS/MOSS-Transcribe-Diarize @ 61bc29cd4120be7b5d3b761b64cd5dff57263642`；torch 2.14.0+cu130、Transformers 5.17.0、Accelerate 1.15.0。

训练 manifest SHA-256：`e2f0e529ef17e2996c753a6ac31b3be11242cbb81cce5dc5fd8e88a3c1ef6c8a`。

## 后续验收

本记录确认训练已经开始，3 epochs 尚未完成。仍需对训练后的模型进行独立评测。交接时服务器 `results/` 中未发现完整零样本 CER/DER/cpCER/DeltaCP 报告，需要补齐；训练 loss 不能证明识别或说话人效果改善。

按评估文档，后续再开展固定 k R-SWA、规则动态窗口、熵门控 RL。当前 SFT 的 loss-only 接口不直接提供 RL 所需的逐 token entropy/log-probability，应在后续单独实现并验证。
