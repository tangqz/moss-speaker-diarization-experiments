# 当前优先任务：63458 + 63472 + 63473 夜间离线任务链

用户将关电脑，约 2026-09-14 中午返回。先读取 `C:/Users/qizhi/Documents/ChatGPT/说话人日志/dkucc/full_attention_v2/overnight/FOLLOWUP.md` 与 submission.json。63458 原搜索之后接 63472 新种子复核、63473 CPU 归档，均已在 Slurm 登记并验证依赖；主搜索完成不代表任务链交付结束。用户预期离线期间，本地 SSH/TensorBoard 断开不等于服务器故障，不重复催登录。

## 原 dev5 路由

先读取 C:\Users\qizhi\Documents\ChatGPT\说话人日志\dkucc\full_attention_v2\dev5\FOLLOWUP.md 和对应 current_run.json。旧 63443 已完成，不得据旧守卫停止新监控。每步 train、每5步完整26场 dev loss/生成，以dev质量早停；正常进展静默，按45分钟检查。

本次 63443 交付已完成：256 文件校验、独立原生报告及四图实际查看完成。预览 http://127.0.0.1:4159/?view=1 。对未改变的完成状态静默，不重复通知或提交新作业。后续 dev5 仍为规范阶段；新 runner 尚未接入。automation_update 未暴露，停止请求已记录，未声称已删除自动化。

# 2026-09-13 新的后续训练要求（优先）

用户要求普通 CE，train loss 每步、完整 26 场 dev loss 每 5 步，以 dev 自由生成准确率指标选 best/停点，并以实验选择学习率。规范在 `dev5/后续训练与学习率选优规范.md` 和 `dev5/protocol.json`；当前为已定义、未接入 runner、未提交新搜索。不得再将六场每 25 步的旧监测称为下一轮方案。保持 full attention 全参数对照和后续 mask/RL 公平性。

63443 已于 2026-09-13 04:42:43 UTC 正常退出，质量门槛失败，停在 50。六场评分齐全：一场截断，另一场严重解析失读，二者非同一会议。完整日志 256 文件已下载校验。报告在 `lr2e6-report/`。下文为历史执行约定，不据其重新提交。

# 当前优先路由：学习率 2e-6 短对照 63443

用户在上一阶段交付后明确要求按方案降低学习率试验。新作业 63443 已提交并进入训练阶段；请先阅读 `C:/Users/qizhi/Documents/ChatGPT/说话人日志/dkucc/full_attention_v2/lr2e6/FOLLOWUP.md` 和 current_run.json。最大执行 50 步，10 步先做完整 TS3004c 质量门槛，完整 dev/test 不在此次短对照内。旧 63421/63441 完成守卫仅属历史，不能阻止新授权工作或停止正在使用的监控。保持 45 分钟、正常进展静默；不得绕过质量门槛。

---

# 当前已转入用户新授权的 full attention SFT v2

## 最新状态：生产入口完整复评 63441 已完成，训练继续暂停

2026-09-13 08:15（北京时间）交付整理。63441 于 07:12:55 完成，Slurm COMPLETED / 0:0，8 项新生成全部完成，三组评分为 12/2/2 条，无执行错误或缺失。相同 base 与 TS3004c 重复记录有重叠，不能视为 16 场独立会议。

修正后 base 的全部 26,337 个生成 token 与历史逐个相同，原始文本也相同。第 50 步仍有 2/6 场截断：TS3004c 连续 1,657 个 Mm.；R8001_M8004 连续 51,141 个相同 token（下）。第 10 步 TS3004c 完整诊断也截断，4,085 个非法 Música 标签，cpCER 99.6092%。AliMeeting cpCER 18.0413% → 25.1156%，AMI 8.9708% → 15.5734%；各自 DER .25 为 8.2830% → 15.8252%、12.2926% → 13.6158%。旧混合预处理比较继续保留为历史诊断。

131 个复评日志已下载并逐文件校验，归档 SHA256 `def0038986fad3350a75ff487c7f2b2c4d8f54531d73579e91218581a48c6e89`。同一个 stage50-report 原生报告已更新，20 份 CSV、7 组 PNG/SVG 保留，新增图与桌面/窄屏、来源面板已实际检查。TensorBoard 连接正常，新增 run `production_eval_63441`；原训练曲线最后一步仍为 50。bridge 每分钟只读刷新继续保留。

**当前没有待检查的训练/评估作业。正式训练未完成，尚未做全部 26 场 dev 与 56 场 test，也未产生合格 SFT。** `diagnostics/下一次学习率单因素短对照.md` 已定义只降低学习率至 2e-6 的后续短对照，尚未提交；本次不自动恢复旧训练、不放宽门槛或修改评分。

完成守卫：先查看 notification-state.json。此完成事件通知一次后，普通心跳不得重复下载、重建报告或重复发结果。监控原因已结束；如果 automation_update 变为可用，按上层要求删除 automation id=moss。本轮工具不可用，已记录 automation_stop_pending，不直接编辑 automation.toml，不声称已经停止自动化；后续仍不可用则 DONT_NOTIFY，不重复告知工具缺失。用户追加新工作时依新请求更新路由。

## 以下为历史路由记录

2026-09-13 06:05 最新路由：63439 早期复核已完成；63440 已实测确认原 v2 与历史/生产音频预处理精度不一致。修复后的完整评估 **63441 正在运行**，训练更新为 0，正式 63421 保持第 50 步暂停。请优先阅读 v2 FOLLOWUP 顶部、diagnostics/current_run.json 及《预处理一致性更正.md》。原生产对照分数暂不用于验收，待六场修正复评；旧输出保留。每 45 分钟正常推进静默，已通知发现不重复；不按后文旧完成守卫停止当前仍有用的监控。

2026-09-13 04:00 最新路由：**63432 推理复核已完成并归档，63439 正在做 base 与 1/10/20 步的早期定位推理，训练更新为 0**。正式 63421 仍保持第 50 步质量暂停。优先阅读 v2 FOLLOWUP 顶部和 diagnostics/current_run.json，保持每 45 分钟；正常推进静默，已通知的暂停和 63432 结果不重复发送。后文 03:00 及旧 v1 信息仅为历史记录，不按旧完成守卫删除当前仍有用的监控。

2026-09-13 03:00 更新：当前正式训练 **63421 已在第 50 步按质量规则暂停**，六场 sentinel 中 AMI TS3004c 出现结构化重复并达到生成上限；Slurm COMPLETED 不代表训练验收通过。阶段报告和全部 247 个日志文件已经整理校验。两卡推理复核 **63432 正在运行**，比较 checkpoint-25 与 checkpoint-50 的同会议输出，训练更新为 0。请先阅读同目录 current_run.json 指向的 `dkucc/full_attention_v2/FOLLOWUP.md`、`current_run.json` 及 `diagnostics/current_run.json`，优先跟进复核结果；正式训练保持暂停，不能擅自绕过质量门槛。以下旧评估交付记录仅作历史保留。本轮任务仍未最终完成，不要按旧完成守卫删除监控或重复旧报告。保持每 45 分钟、无重要变化则静默；已知第 50 步暂停不重复通知。

---

# 交付已完成（2026-09-12）

评估 63363 已 COMPLETED / 0:0，164/164 评分完成，missing=0、errors=0。327 个日志与结果文件已下载并逐文件校验，报告五组图已实际查看，桌面与窄屏页面、异常展开和四个本地下载链接均已验证。交付在 `dkucc/artifacts/eval-62994-63363`，证据见 `delivery-verification.json`。该文件记录当前工具限制：本轮未暴露 `open_in_codex` 或 `automation_update`，所以未能打开 Codex 面板或停止自动化；不要声称已暂停。

后续心跳请先处理停止自动化：搜索可用的 `automation_update`，有工具则按当前上层心跳规则使用 `mode="delete"`、id=`moss`（若上层要求暂停，则用结构化工具保留其他字段并设 PAUSED）。不要直接编辑 automation.toml，不要重新评估、重新下载或重建已经验收的报告。若工具仍不可用，维持完成记录并返回 DONT_NOTIFY，避免重复交付或重复工具缺失通知。用户明确追加新任务时再按新任务继续。

本地预览服务器端口 4173；HTML 为 `report/MOSS_训练与评估报告.html`。下载压缩包 SHA-256：`410dfcc9130bc10caf03717585446018951772e876833d7d2d3ff22eaed5f814`。最终报告中的三测试集结果：AMI 改善，AliMeeting / AISHELL-4 退步；SFT 截断 9 条、空解析 2 条，其中 1 条重叠（共 10 场会议）；原始模型均为 0。

---

# 评估结束后的本地交付

当前运行信息以 `current_run.json` 为准。正式评估作业 63363；之前 63361 在权重文件名预检处退出，没有完成任何预测。训练作业 62994 已正常完成。

服务器运行目录：`/work/qt28/moss/results/eval-62994-63363`。评估对原始模型和训练后模型各运行 82 场完整会议，共 164 条预测。当前共享 SSH 路径为 `C:/Users/qizhi/.ssh/cm-moss-check-20260912`。

检查 Slurm 状态、`worker-*-status.json`、`job_exit.json`、`metrics_summary.json`。日志与预测实时保存在运行目录。对正在正常推进的作业保持安静；仅异常、需要登录或最终交付时通知用户。若 SSH 断开，先检查是否有其他已授权的有效连接，再通过 `check_training_interactive.ps1` 的交互认证方式恢复；不要向用户索取聊天中的密码或 Duo 信息。已有登录脚本会查询旧训练作业，但保持的新 SSH 连接仍可用于本次评估。

完成判定：Slurm `COMPLETED` / `0:0`，`job_exit.json` 的 `exit_code=0`，`metrics_summary.json` 的 `complete=true`，`scored_predictions=164`，无缺失或推理错误。若存在明确异常，保留原始输出，修复实现并按相同模型、输入和评分协议恢复。不要开始新训练，不要为了改善测试结果改变协议，也不要把部分结果当作完整结果。

启动阶段已对前三条真实预测执行了一次评分核验，得到 scored=3、errors=0、missing=161。评分器对尚未完整的结果按设计返回 2，因此这个独立 srun 步可能在 Slurm 历史中显示非零退出码；这不代表四个推理 worker 或主作业失败。应看主作业和最终完成标记。跟进自动化 ID 为 `moss`，每 45 分钟检查一次。

2026-09-12 13:05 轮询发现了模型输出质量异常，推理进程仍正常推进：SFT 在 `alimeeting/dev/R8001_M8004` 生成至 65536 token 上限，末段持续重复“你”，只有前 170 个完整片段被官方解析器提取；同会议 base 正常 EOS、504 个片段。SFT 在 `ami/dev/TS3004b` 输出有完整文字，但开头出现重复 `[34.65][34.65]` 时间戳，使官方解析器返回 0 个片段。原始证据另存本地 `anomaly-check-63363.json`。这些属于本次固定模型和官方解析协议下的结果，应保留并在报告中说明，不能为了分数更好改写预测或中途换解析器。CER/cpCER 只评估官方解析出的片段，因此不能单独代表未经解析的重复尾部质量。用户已获知这类异常；后续相同模式的数量变化无需逐次通知，继续记录，最终完整报告时汇总。新增进程故障、需要认证或最终交付仍应通知。

完成后在本地项目依次执行：

```powershell
python dkucc\evaluation\download_results.py --job 63363
python dkucc\evaluation\build_report.py --artifact dkucc\artifacts\eval-62994-63363
```

下载程序会在服务器创建压缩包，然后下载并验证整个压缩包及每个文件的 SHA-256。内容包括项目全部训练日志、本次评估日志、逐条预测、原始文本与 token IDs、评分明细、训练状态与运行配置；不包括大模型权重和原始音频。原始内容位于 `dkucc/artifacts/eval-62994-63363/raw`。

报告生成程序输出完整离线 HTML 报告、五组 PNG/SVG 图表、汇总 CSV。必须检查生成成功、报告引用文件存在，并用 `view_image` 实际查看图表，检查文字、图例、刻度与数据一致性。必要时修复后再交付。可以用浏览器或 PDF 预览作进一步版式检查；未做的检查不要声称完成。

最终交付链接：

- `dkucc/artifacts/eval-62994-63363/report/MOSS_训练与评估报告.html`
- `dkucc/artifacts/eval-62994-63363/eval-62994-63363-all-logs.tar.gz`
- `dkucc/artifacts/eval-62994-63363/report/aggregate_metrics.csv`

最终答复报告三测试集的 CER/DER/cpCER 变化、截断或异常数量，以及本地日志校验结果。区分训练完成、单次固定模型评估与“已证明收敛”。打开报告到 Codex 面板。完成这一次交付后暂停跟进自动化。

