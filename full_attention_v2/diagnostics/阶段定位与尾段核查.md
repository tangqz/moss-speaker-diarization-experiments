# 第 50 步暂停后的阶段定位

2026-09-13。延续已批准方案末尾的顺序：精度修正后仍有灾难性重复，先用中间保存点定位，再决定是否进行学习率或 warmup 单因素短对照。正式训练 63421 暂停在 50；当前没有新训练更新。

## 已完成的 63432

同一 TS3004c 完整音频、原 BF16 greedy 入口和 65,536 token 上限。第 25 步和第 50 步复核均达到上限，分别有 1,692 / 1,695 个连续 “Mm.” 片段。第 50 步两次生成的全部 token IDs、原始文本和模型来源一致。对该会议，回退到第 25 步不解决循环；这不是所有样本的确定性或总体退化证明。

日志归档 `../artifacts/63432/logs-63432.tar.gz`：34 个文件，460,616 bytes，SHA256 `158b1f4582e121bdfad9e22d3892c08662c0aa5f68d094a7616c65ec34c80e3d`。四条比较评分是两个 SFT 输出分别与同一历史 base 比较，不能当作四场独立会议。

## 63439：已有权重的四路推理

| 候选 | 作用 | 参数更新 |
|---|---|---:|
| 原始 base | 用同一冻结入口复核历史 base 结果 | 0 |
| checkpoint-1 | 核查第一次更新后的行为 | 0 |
| checkpoint-10 | 定位更早的观测点 | 0 |
| checkpoint-20 | 衔接已知失败的第 25 步 | 0 |

Slurm 四张 A40、2 小时上限；完整音频和评分保持原样。权重只读加载，1/10 步不要求已清理的 optimizer 状态。每个候选均保存实际权重/配置/source hashes。冻结生成脚本统一将候选写到 `predictions/sft`：**checkpoint-0 中这个标签指重新运行的原始 base，不是训练后模型**，分析与报告必须据 `candidate_kind` 正确命名。

GPU index 3启动时已有约 24.4 GiB 占用；当前推理仍正常推进，此处不作吞吐或独占硬件速度比较，不终止其他进程。如其发生显存错误，保留日志，仅对未完成候选按相同协议恢复到可用卡，已经完成的输出不要重写。

完成时核查 Slurm、job_exit、outcome、四个候选各 2 条完整评分，无执行错误或 missing，并下载全部日志。每个候选的质量失败是有效诊断结果，不应被当作程序错误重跑。

## 如何解释下一批结果

- 若重新运行的 base 与历史输出一致，而早期 SFT 已失败，则推理入口的可比性更明确；首次观察到失败的保存点给出阶段范围，不证明该区间只有一次转变或之后必然一直失败。
- 若 base 复核与历史不一致，先核对权重、processor、音频、模型代码和推理参数，不能直接把差异归因于训练。
- 某个早期权重在单场通过，不代表已通过六场 sentinel 或全 dev；不将其临时加入正式 checkpoint 选择，也不借此放行当前训练。
- 当前 teacher loss 改善并不能解释循环成因；不能仅凭自由生成失败断言学习率过大、声学编码器被破坏或经典过拟合。

## 音频与参考覆盖待核查

TS3004c 的最后参考标注止于 2,452.69 秒，音频长 2,970 秒，尾部 517.31 秒没有参考段；第 25/50 步循环起点为 2,502.35/2,502.38 秒。需核对这段是否有真实语音、非会议交谈、静音或其他声音，以及原参考如何定义标注范围。当前尚未听审，不能把“无标注”写成“静音”，也不能把 base 在尾段的全部输出默认当作幻觉。

保持原完整音频和既定评分作为正式结果；任何按参考覆盖裁剪、重标注或辅助评分只能另列诊断协议，不能覆盖旧结果或隐含改变 full/mask/RL 对照。完成这些核查后再准备具体的单因素训练对照，保留同一 base、完整数据、有效 batch、更新精度和 402 步时钟；不能绕过当前暂停条件恢复正式更新。
