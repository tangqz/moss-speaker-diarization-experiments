# MOSS full attention SFT v2 执行记录

## 2026-09-13 已执行学习率单因素短对照：63443

用户明确要求“按你的方案改变学习率试试看”后，提交独立作业 63443，从同一原始 base 以 lr=2e-6、warmup=0、402 步线性调度开始，最多执行 50 步。保留全参数 full attention、FP32 参数/梯度/Adam、BF16 前向、相同数据与有效 batch。训练代码仅三处 1e-5 改为 2e-6；额外改动是早期保存点与第 10 步恢复边界验证，代码差异在 lr2e6/bundle/trial_changes.diff，源文件 SHA256 与提交收据保留。

实际节点 dkucc-core-gpu-dkurc-d-it09-8，4 A40、32 CPU、384 GiB。启动检查已通过：原 base 权重/配置/模型源码、训练数据、关键依赖版本和四卡拓扑与相应证据相符。已经独立核对前三步的会议索引、各监督 token/全局分母与旧 63421 完全一致，LR 是对应旧步数的 1/5；参数和优化器精度正确，首步七组参数抽查 lost=0。TensorBoard 已读回第 4 步 loss/LR，HTTP 200；名称 lr2e6_full_attention_63443。

第 10 步完整 TS3004c 失败即停，通过才继续到第 25/50 步；第 50 步原六场完整生成后停止审阅，不自动执行全 dev/test 或继续到 134。基线 teacher fast-0 复用自 63421，生产生成脚本使用已在 63441 完成复评的版本。旧训练与复评文件保留；本次完整信息与后续处理见 lr2e6/FOLLOWUP.md、current_run.json 和 launch-verification.json。既有 45 分钟监控路由已切换，工具不可用时不直接编辑自动化配置。

以下保留此前阶段的历史记录。

2026-09-12，按已确认的《下一轮FullAttention_SFT方案》执行。首次正式作业 63415 在完成 15 步后发生主机内存超限。修复后的当前作业为 **63421**，从同一原始 base 重启。实时信息以 `current_run.json`、`latest_status.json` 和服务器日志为准。

当前 TensorBoard：[http://127.0.0.1:6006](http://127.0.0.1:6006)，正式记录名称 `full_attention_v2_63421`。图表每分钟刷新；主动跟进仍为每 45 分钟，有重要变化时通知。Text 页签显示训练状态；旧作业曲线保留。

## 最新状态：生产入口完整复评 63441 已完成，训练继续暂停

2026-09-13 08:15（北京时间）交付整理。63441 于 07:12:55 完成，Slurm COMPLETED / 0:0，8 项新生成全部完成，三组评分为 12/2/2 条，无执行错误或缺失。相同 base 与 TS3004c 重复记录有重叠，不能视为 16 场独立会议。

修正后 base 的全部 26,337 个生成 token 与历史逐个相同，原始文本也相同。第 50 步仍有 2/6 场截断：TS3004c 连续 1,657 个 Mm.；R8001_M8004 连续 51,141 个相同 token（下）。第 10 步 TS3004c 完整诊断也截断，4,085 个非法 Música 标签，cpCER 99.6092%。AliMeeting cpCER 18.0413% → 25.1156%，AMI 8.9708% → 15.5734%；各自 DER .25 为 8.2830% → 15.8252%、12.2926% → 13.6158%。旧混合预处理比较继续保留为历史诊断。

131 个复评日志已下载并逐文件校验，归档 SHA256 `def0038986fad3350a75ff487c7f2b2c4d8f54531d73579e91218581a48c6e89`。同一个 stage50-report 原生报告已更新，20 份 CSV、7 组 PNG/SVG 保留，新增图与桌面/窄屏、来源面板已实际检查。TensorBoard 连接正常，新增 run `production_eval_63441`；原训练曲线最后一步仍为 50。bridge 每分钟只读刷新继续保留。

**当前没有待检查的训练/评估作业。正式训练未完成，尚未做全部 26 场 dev 与 56 场 test，也未产生合格 SFT。** `diagnostics/下一次学习率单因素短对照.md` 已定义只降低学习率至 2e-6 的后续短对照，尚未提交；本次不自动恢复旧训练、不放宽门槛或修改评分。

## 2026-09-13 第 50 步质量暂停与阶段交付

06:05 追加更正：63439 已全部完成。原 v2 预处理下 base/第 1 步正常 EOS；第 10 步出现大规模时间戳/非法标签循环（4,666 个非法标签、仅 10 个合法片段、cpCER 99.492%），第 20 步为 1,643 个连续 “Mm.”（cpCER 22.763%），两者均截断。60 个文件归档 hash `a973f0c622a62f03f434e17e8d06341526b7d15793ff6a7e7ed2e6334efca47f`。

进一步发现原生成脚本漏掉了历史/生产音频预处理使用的 CUDA BF16 autocast 环境。63440 已用固定音频/权重的单因素探针确认：最终 feature 都是 float32，但数值不一致；恢复生产环境后 base 前 512 token 与历史完全一致。第 10 步两种环境仍出现非法标签重复，因此不能把问题全归于精度差异。13 个探针文件归档 hash `95d7aac5e2dd6bff58958887775b5a6abc7681b28c5c1bd49311d1d3c9296d6e`。此前“完全一致推理协议”的表述需要更正，旧生产对照差值暂不用于验收；训练/teacher 的同协议结果保持有效。

派生生成入口只修复该上下文并记录元数据，差异见 `diagnostics/preprocessing_fix.diff`；原文件和结果保留。完整复评 **63441** 已于 05:59 启动，六场第 50 步 + TS3004c 新 base 与第 10 步，共 8 项新生成。四路实际进展已确认，正式训练仍暂停，训练更新为 0。报告顶部及 TensorBoard Text 状态均明确显示协议更正；长期 full/mask/熵 RL 的输入复用要求见 `diagnostics/预处理一致性更正.md`。

04:00 追加：推理复核 63432 已于 03:34 完成（COMPLETED / 0:0，时长 58:32）。第 25 步和第 50 步在 TS3004c 均达到 65,536 token 上限；连续 “Mm.” 分别 1,692 / 1,695 段。第 50 步与首次输出的全部 token IDs、原始文本和权重来源完全相同。单场 cpCER：历史 base 12.438%、第 25 步 22.630%、第 50 步两次均 22.571%。两组各 2 条评分完整，无 errors 或 missing，不视为四场独立样本。34 个日志文件已下载并校验，归档 hash `158b1f4582e121bdfad9e22d3892c08662c0aa5f68d094a7616c65ec34c80e3d`；原生报告和导出图已追加复核，原首次 HTML 留存。

该会议最后参考标注结束 2,452.69 秒，音频 2,970 秒，循环起点约 2,502 秒。尾部音频与标注覆盖需单独核查；当前未听审，不能称其静音。为按已批准方案定位更早变化，推理作业 63439 已于 03:53 启动，四张 A40 分别加载原始 base 与现有 1/10/20 步权重。04:00 四路均推进；GPU index 3 原有约 24.4 GiB 占用，这批不作吞吐对照。训练更新为 0，正式训练仍暂停。具体协议与分析边界见 `diagnostics/阶段定位与尾段核查.md`。

01:50（北京时间），63421 在第 50 步完整 sentinel 评估后按预定规则暂停，作业正常退出，Slurm COMPLETED / 0:0，运行 1:51:14。12/12 条 base/SFT 预测均已评分，无执行错误或缺失；SFT 有 1 场截断，其余 5 场正常 EOS，无空解析或严重解析失读。当前未完成 26 场全 dev、56 场 test 或整轮训练，不能宣称收敛或合格。

AMI TS3004c 的 SFT 输出含连续 1,695 个 “Mm.” 片段，交替写入说话人与时间戳，最终耗尽 65,536 token。对应 base 为 26,337 token、正常 EOS。合法片段解析未出现严重损失；单 token 连续重复长度只有 6，遗漏了结构化循环的程度。新增连续文本片段统计用于诊断，原评分不变。

| 六场固定 sentinel 指标 | Base | 第 50 步 |
|---|---:|---:|
| Teacher loss（全部） | 0.59307 | 0.42480 |
| AliMeeting cpCER | 18.04% | 19.86% |
| AMI cpCER | 8.97% | 15.26% |
| AliMeeting DER（0.25 s collar） | 8.28% | 9.48% |
| AMI DER（0.25 s collar） | 12.29% | 13.29% |

Teacher loss 改善与自由生成退步同时存在；仅凭这些结果还不能把原因定为过拟合。AliMeeting 三场均正常结束但分数也退步，说明问题不只来自单场循环。此前主机内存问题已在实际 50 步通过原第 14/16 步故障位置，这次暂停是生成质量触发。

03:00 已将本次全部 247 个训练和阶段验证日志下载并逐文件校验，归档 `artifacts/63421/logs-63421.tar.gz`（15,469,398 bytes），SHA256 `382a056a622a962af05c1a180296a71b3096ef4448a40a75ac7f2e8cfd578105`。这是该次已退出尝试的完整日志，不包含尚未发生的后续训练或测试。阶段报告 `stage50-report/MOSS_50步阶段报告.html` 与原生报告 `stage50-report/app` 已完成；5 个交互图、四组 PNG/SVG、7 个 CSV、来源说明与失败样例齐全。实际查看桌面、窄屏、来源面板和导出图，证据见 `stage50-report/verification.json`。

另启动推理复核 63432：checkpoint-25 和 checkpoint-50 在同一 TS3004c 上分别比较与重复，保持原 BF16 greedy 完整音频生成和评分，使用两张 A40，不进行训练更新。结果目录 `/work/qt28/moss/results/full-attention-v2-diagnostic-63432`。03:00 两 worker 仍正常生成，尚无最终结果；正式训练继续暂停。其路由和后续检查规则见 `diagnostics/current_run.json` 及 `FOLLOWUP.md`。

## 2026-09-13 内存故障修复

9 月 12 日 23:25（北京时间），63415 在第 16 步期间被 Slurm 判定 OUT_OF_MEMORY，MaxRSS 正好达到请求的 240 GiB。它已完成 15 步；首次保存点为 25，因此没有可恢复权重。SSH 和 TensorBoard 均持续工作，停止的是训练本身。83 个日志与诊断文件已归档并逐文件 SHA256 验证，归档 hash 为 `bd37629bc774478f9e5d16146adfbdef7bba9a2e108099a597e0516f9c7959ed`。

最长会议位于第 14 步，rank 3 历史 RSS 由 61.8 升至 112.9 GiB；四进程各自的缓存高水位累积。主机内存超限有直接调度器证据；闲置 pinned 缓存滞留是结合代码与后续释放实验定位的主要原因，仍需观察经过原故障批次后的长期占用。

新入口 `host_runtime.py` 在每步更新完成后同步 GPU、回收 Python 循环引用、释放已闲置的 pinned host 缓存，并核对 RNG 不变。原 train.py、memory_sft.py 和实验输入协议保持 hash 一致；模型、数据、loss、学习率时钟、attention 和评估标准保持相同。另申请 384 GiB 主机内存，并增加第 1/10/20 步完整保存点。小型 CUDA 验证已证实闲置缓存确实归还、活跃张量内容不变，loss、梯度和 RNG 完全一致；实际训练还会逐步记录释放前后占用。接口依据：[PyTorch 2.14 主机缓存释放](https://docs.pytorch.org/docs/2.14/generated/torch.accelerator.memory.empty_host_cache.html)。

63421 是从 base 重启的独立尝试，不拼接 63415 的 15 步充当恢复训练。修复只涉及内存管理、监看和保存，不改变后续 mask KV SFT 与熵 RL 所需的对照条件。

截至 9 月 13 日 00:06（北京时间），新作业连续完成 4 步，第 1 步的权重、optimizer、scheduler 和四卡 RNG 保存完整。四 rank 每步记录的 RSS 合计在清理前约 89.7–108.0 GiB、清理后约 9.1–12.4 GiB；pinned 缓存合计从约 77.8–99.1 GiB 降至 0.22–0.44 GiB。这里是各卡在步末采样值的合计，不等同于同一瞬间的 Slurm 峰值。实际数据顺序、全局监督 token 分母、FP32 更新检查及 6 场 step-0 基线已独立核对；第 1 步 loss 与旧尝试相同，后续小差异不能解释为模型质量变化。TensorBoard 的 3 个已同步真实训练点、基线及状态页已验收。第 14/16 步尚待执行，故不宣称已验证整个长会训练过程稳定。证据见 `recovery/verification-63421.json` 与 `launch-verification.json`。

## 已落实的配置

9 月 13 日 00:54 更新：63421 已完成 50 步，25/50 步 teacher 验证完成，正在第 50 步六场完整自由生成。第 14 步最长会议处理后的各 rank RSS 合计从 151.47 GiB 回落至 12.30 GiB；第 16 步从 114.70 回落至 12.43 GiB，已通过此前故障位置。50 步数据顺序和全部全局 token 分母已核对，累计监督 token 4,076,380。完整恢复点现保留 20/25/50；1/10 保留权重并按既定策略清理优化器状态。已归档 233 个文件（约 15.1 MB），SHA256 为 `e825ee1608d6b1a6fc3d2edf64cff3a4acd39b427f9c640cf850b608c1efff33`。这仍是运行中快照，完整生成质量尚待验证。

原 536 场完整会议、全参数、full causal SDPA、DDP4 / SP1、有效 batch 4、普通 next-token CE。常驻参数、梯度和 Adam 状态使用 FP32，前向 BF16 autocast，CE 和概率统计 FP32。学习率 1e-5、linear 402 步，无 warmup 和 weight decay。没有裁短会议、冻结编码器或加入反重复目标。

先到 134 步，按完整 dev 验证和原定早停规则决定继续至 268/402。每 25 步保存权重并进行六场完整音频 teacher loss / token 类型 / 熵检查；第 50 步六场完整生成；第 134/268/402 步全部 26 场 dev 生成。选择冻结后才运行 56 场 test。原 parser 与失败会议计分方式保持不变。

Trainer 在各边界停止后，由同一 Slurm 作业内的流程恢复 optimizer、scheduler 和各卡 RNG；总学习率时钟固定 402。训练、验证进程依次运行，以便验证始终使用生产 BF16 推理路径，同时保留 FP32 训练权重。所有实际 batch index、有效 token 分母、逐步更新摘要和原始预测均保存。

## 启动验收

- 四卡完成包含 **116527 token** 的最长会议更新，单卡最大显存 **28.263 GiB**。
- 683 组可训练参数及 1366 个 Adam 动量张量均为 FP32；共享 embedding/head 保持绑定。
- 全部 **908513280** 个参数的序列化值与保存前一致。
- AMI IS1008a 完整音频前缀（12428 token）上，FP32 保存权重转 BF16 与直接 BF16 重载的 logits 差为 **0**。
- 恢复后各卡数据、RNG 和 402 步学习率时钟一致。另用实际 Trainer/Accelerate sampler 验证全部 19 个阶段恢复边界，并在每个正式训练阶段结束核对实际数据顺序。
- 新输出诊断对旧 164 条预测重放，与之前诊断的字符数和严重解析失读判定全部一致；选择、早停、token 类型与完整词表统计检查通过。

### 数值一致性的边界

| 对照 | loss 差 | 抽样梯度相对 L2 差 |
|---|---:|---:|
| 四卡 vs 单卡累积 4 | 0 | 0.71095% |
| 相同四卡配置重复 | 0 | 0.86728% |
| 连续训练 vs 断点恢复 | 0 | 0.38279% |
| 相同断点恢复重复 | 0 | 0.32482% |

最初设置的 0.5% 梯度阈值和逐位训练复现要求过严。随后保留原结果，并加做相同配置重复对照；相同配置自身的波动与跨路径差异处于同一量级。最终要求全局与跨路径各参数张量抽样误差小于 3%，跨路径误差还须小于实测重复波动的 3 倍加 0.1%；没有把每个接近零的重复控制分量都要求固定相对精度。

连续与恢复的全参数权重相对差约 **9.40×10⁻⁷**，相同恢复重复约 **9.09×10⁻⁷**；相对于本步参数更新的差异分别为 **0.773% / 0.748%**。因此验收证明保存值精确、恢复语义一致并处于已测数值波动范围，**不宣称训练逐位可复现**，也不把这些工程检查当作质量或收敛证明。相关计算边界见 [PyTorch 数值精度说明](https://docs.pytorch.org/docs/2.14/notes/numerical_accuracy.html)。

## 尝试与证据保留

| 作业 | 结果与原因 |
|---|---|
| 63405 | 源码校验字段误用了内存训练模块的 hash，未开始更新；核对既有源码快照后修正 |
| 63406 | 完成四卡、最长会议、单卡累积和恢复；严格 0.5% 梯度门槛触发 |
| 63408 | 节点一张卡已有约 22 GiB 占用，显存预检拦截，未更新；后续排除该节点 |
| 63410 | 完成相同配置重复对照；某个重复控制张量差 3.079% 触发过严分量门槛，整体仅 0.325% |
| 63412 | 数值和保存检查通过；完整音频重载探针的设备参数类型错误，未完成最后探针 |
| 63413 | 依赖上述失败验收，正式训练从未开始即取消 |
| **63414** | **最终验收 COMPLETED / 0:0** |
| 63415 | 完成 15 步，第 16 步主机内存超出 240 GiB；没有完整保存点，失败日志保留 |
| **63421** | **加入闲置主机缓存释放后，从相同 base 重启的正式训练** |

数值训练入口在重复验收间保持相同；重试修正的是诊断脚本、数值判据和节点选择。原始失败日志、重复对照、最终验收和来源 hash 均保留。`artifacts/` 内为已下载并逐文件 SHA256 校验的记录；`launch-verification.json` 记录正式更新和实际数据顺序核查。

TensorBoard 使用本地独立环境，仅读取这些日志，不修改服务器训练环境。训练运行及完成后下载、报告交付的跟进规则见 `FOLLOWUP.md`。逐步曲线使用真实损失分子/分母；callback 提前暂停时 Trainer 汇总的吞吐和跨阶段平均 loss 不直接作为整轮指标。
