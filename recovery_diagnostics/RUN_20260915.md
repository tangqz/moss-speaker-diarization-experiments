# D0/D1 execution record — 2026-09-15

- Active Slurm job: `63691`
- Node after scheduling: `dkucc-core-gpu-dkurc-d-it09-3`
- Requested resources: 4 × NVIDIA GPU, 384 GiB RAM, 4-hour limit
- Remote experiment root: `/work/qt28/moss/results/ms-swift-63643/evaluations/recovery-diagnostics-d0-d1`
- Remote source snapshot: `/work/qt28/moss/dkucc/recovery_diagnostics_20260915`
- Submission reused the existing authenticated SSH proxy at the user's request; no new login was opened.

## Verified live progress

- Job `63691` entered `RUNNING` on `dkucc-core-gpu-dkurc-d-it09-3`.
- Transformers BF16 D1 completed all 52 replays (4 models × 13 fixed histories) with zero runtime failures.
- The job then entered the bounded Transformers FP32 stage for Base and Step 90. Live GPU checks showed active computation without OOM or traceback.
- After FP32, the same job will automatically run repeated vLLM BF16 D0 cases and produce `summary.json`, `summary.md`, and the final tar archive.

## Superseded attempt

Job `63690` was cancelled after the first completed generations exposed a report-only parser field mismatch (`TranscriptSegment.end_time` versus the actual `TranscriptSegment.end`). Its partial evidence was preserved at:

`/work/qt28/moss/results/ms-swift-63643/evaluations/recovery-diagnostics-d0-d1-attempt-63690`

The corrected `hf_replay.py` was checked locally and remotely with matching SHA-256:

`49d8bd47efed9872f0a00edbe4ffa8126e9b88c3522e29072ad3506f5bdc235c`

The mismatch occurred after generation while extracting the last parsed timestamp; it did not change model inputs, weights, logits, or decoding. Job `63691` starts from a fresh result directory so no failed records can be mixed into the corrected summary.
